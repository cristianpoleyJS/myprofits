import FormStock from 'components/forms/FormStock'

const AppLayout = () => {
    return (
        <header className="App-header">
          <FormStock />
        </header>
    )
}

export default AppLayout